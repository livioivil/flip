flip

======

dev for R package flip.

Install it in R using the following command

# install.packages("devtools") # if you don't already have it.

library(devtools)

install_github("flip", "livioivil")

library(flip)
